<ul>

<h1><%= @user.first_name %></h1>
<h1><%= @user.last_name %></h1>
<h1><%= @user.description %></h1>

<%= @user.email %>

  <a <%= link_to "Tu peux modifier ton mot de passe ici", edit_user_registration_path %> </a>

<a> ci_dessous les évènements que tu as créé </a>
 <%= @events.each do |event| %>
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title"> <%= event.title %> </h5>

    <p class="card-text"> <%=  event.description %> </p>
    <a <%= link_to "accéder à la page de l'évènement", event_path(event) %> 
 
    </div>
       </div>
        <p> </p>
               <% end %>



</ul>

